{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h3>{{ exercise.title }}</h3>

            <p>{{ exercise.description }}</p>
        </div>
    </div>
{% endblock %}
